# analysis_options.yaml
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    avoid_print: false                        # We use print for debug logging
    prefer_const_constructors: false          # Too noisy, not critical
    prefer_const_literals_to_create_immutables: false
    deprecated_member_use: false             # withOpacity deprecation - cosmetic only